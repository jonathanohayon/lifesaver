{
  "name": "@lifesaver/web",
  "version": "0.8.5",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite src --host 127.0.0.1 --port 3000",
    "build": "node ../../scripts/build-web-static.mjs",
    "preview": "npx serve ../dist"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "description": "LIFE.SAVER Web - v0.8.5 Claude backend compatibility metadata."
}
